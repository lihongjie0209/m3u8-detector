name: Release

on:
  push:
    tags:
      - 'v*.*.*'  # 触发条件：推送版本标签（如 v1.0.0）

permissions:
  contents: write  # 需要写权限来创建release

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout代码
        uses: actions/checkout@v4
        
      - name: 设置Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: 安装依赖（跳过可选依赖）
        run: |
          npm ci --ignore-scripts
          
      - name: 使用预生成的图标
        run: |
          echo "✅ 使用项目中已有的PNG图标"
          ls -lh icons/*.png
          
      - name: 验证扩展文件
        run: npm run validate
        
      - name: 构建扩展包
        run: npm run build
        
      - name: 获取版本号
        id: get_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "📦 版本: ${VERSION}"
          
      - name: 获取更新日志
        id: changelog
        run: |
          if [ -f "CHANGELOG.md" ]; then
            echo "读取CHANGELOG.md..."
            # 提取当前版本的更新内容
            CHANGELOG=$(sed -n "/## \[${{ steps.get_version.outputs.version }}\]/,/## \[/p" CHANGELOG.md | sed '$ d' | tail -n +2)
            if [ -z "$CHANGELOG" ]; then
              CHANGELOG="发布 ${{ steps.get_version.outputs.version }}"
            fi
          else
            CHANGELOG="发布 ${{ steps.get_version.outputs.version }}"
          fi
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
      - name: 创建GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ steps.get_version.outputs.version }}
          body: |
            ## 🎉 M3U8资源探测器 ${{ steps.get_version.outputs.version }}
            
            ${{ steps.changelog.outputs.changelog }}
            
            ### 📦 安装方法
            
            1. 下载下方的 `m3u8-detector.zip` 文件
            2. 解压到本地目录
            3. 打开Chrome浏览器，访问 `chrome://extensions/`
            4. 开启右上角的"开发者模式"
            5. 点击"加载已解压的扩展程序"，选择解压后的目录
            6. 完成安装！
            
            ### 🔗 相关链接
            
            - [使用文档](https://github.com/${{ github.repository }}/blob/main/README.md)
            - [快速开始](https://github.com/${{ github.repository }}/blob/main/QUICKSTART.md)
            - [问题反馈](https://github.com/${{ github.repository }}/issues)
            
          files: |
            dist/m3u8-detector.zip
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: m3u8-detector-${{ steps.get_version.outputs.version }}
          path: dist/m3u8-detector.zip
          retention-days: 90
